/* ====================================================================
   Journal — placeholder screen.
   The real journal module lands in a future build. For now this
   surface exists so the home grid balances and the user can see the
   tile destination wired up.
==================================================================== */

export default function JournalScreen() {
  return (
    <div className="journal-placeholder">
      <p className="journal-placeholder-line">Coming soon.</p>
      <p className="journal-placeholder-sub">
        A daily journal lives here next. One question, an open page, and a quiet record of what you noticed.
      </p>
    </div>
  )
}
